# v1.8.2 Plan — Rebuild from Truth

## The Truth
APIServer2.0.js is the only source of truth. A previous AI session modified it directly, disconnecting it from the harness api-component. The api-component was never updated to match. That's why we rebuild from the server.

## Flow
APIServer2.0.js → inject into audit Q v1.8 harness → TEST → export to api-component.js → export to apiserver

## Steps

1. Take APIServer2.0.js (the truth), inject into audit Q v1.8 as @@@@ APISERVICE_COMPONENT
2. mcp-engine as @@@ sub-component, ai-engine as @@@ sub-component
3. Default username/password in app (logon comes later)
4. TEST in the harness — chain fires, grid fills, Online works without CORS
5. Once harness passes, export to api-component.js
6. Export to APIServer2.1.js

## Markers
- @@@@ = components (api-component, irrigation-component, dashboard-component)
- @@@ = sub-components inside a component (mcp-engine, ai-engine)

## NOT touched
- irrigation-component

## Rule
Test in the harness first. Nothing gets exported until it works here.
